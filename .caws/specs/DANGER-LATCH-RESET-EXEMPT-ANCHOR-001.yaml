id: DANGER-LATCH-RESET-EXEMPT-ANCHOR-001
title: 'Reset-exemption end-anchor: the trap exit admits exactly one simple reset invocation'
risk_tier: 1
mode: fix
lifecycle_state: closed
resolution: completed
closure_notes: 'Auto-closed by caws worktree merge wt-reset-anchor at 48332f87e20e6330f95d879f821ff74ba26c7604'
created_at: '2026-09-12T19:49:28.982Z'
updated_at: '2026-09-12T19:51:51.936Z'
created_by_session: 'session-c04418cd-a035-44a9-b26e-378d285ccad1'
blast_radius:
  modules:
    - 'packages/caws-cli/templates/hook-packs/shared/block-dangerous.sh (is_reset_latch_invocation)'
    - 'packages/caws-cli/tests/hooks/bats/block-dangerous.bats'
  data_migration: false
scope:
  in:
    - packages/caws-cli/templates/hook-packs/shared/block-dangerous.sh
    - packages/caws-cli/tests/hooks/bats/block-dangerous.bats
  out:
    - packages/caws-cli/src
    - packages/caws-cli/dist
invariants:
  - 'The reset exemption (the trap''s only exit) admits exactly ONE simple invocation: optional benign prefixes (cd <dir> &&, VAR=value assignments, env VAR=value), the reset script optionally via bash/sh/. with a path, arguments containing no command separators or substitution metacharacters (; & | $ backtick parentheses redirects), and NOTHING after the invocation — the match is end-anchored.'
  - 'Ported from consumer hardening STERLING-LATCH-RESET-PREFIX-EXEMPTS-UNEXAMINED-REMAINDER-01: the pre-fix prefix-match exempted "reset-danger-latch.sh ... && <arbitrary remainder>" from the trap, a live escape once the reset became the sole exit path (DANGER-LATCH-QUARANTINE-TRAP-001).'
  - 'Denial semantics for non-exempt reset-shaped compounds are unchanged: block decision, strike recorded, escalation path untouched.'
acceptance:
  - id: A1
    given: 'a session with an armed trap sentinel'
    when: 'it runs the plain reset invocation (bash .caws/hooks/reset-danger-latch.sh --session <id> --reason x)'
    then: 'the guard exits 0 and admits the command (the exemption still works for the simple form)'
  - id: A2
    given: 'a session with an armed trap sentinel'
    when: 'it runs the reset invocation prefixed by benign forms (cd <dir> && ... or env CAWS_MACHINE_RUNTIME=1 CAWS_PROJECT_DIR=/abs ... bash <snapshot>/reset-danger-latch.sh, the emitted machine recovery shape)'
    then: 'the guard exits 0 and admits the command'
  - id: A3
    given: 'a session with an armed trap sentinel'
    when: 'it runs a reset-shaped command with a compound remainder (reset-danger-latch.sh --session x --reason y && rm -rf /, or ... ; git push, or ... | sh)'
    then: 'the guard emits a block decision and records a strike — the remainder is NOT exempted'
  - id: A4
    given: 'a session with an armed trap sentinel'
    when: 'it runs a reset invocation whose arguments carry substitution metacharacters ($, backtick) or redirection'
    then: 'the guard emits a block decision (the exemption admits separator-and-substitution-free arguments only)'
observability:
  - 'Every denied reset-shaped compound is recorded as a trap strike with the sentinel and escalation audit log entries unchanged from TRAP-001.'
rollback:
  - 'Revert the implementation commit; the prior prefix-match semantics return; rerun the block-dangerous bats suite plus caws doctor.'
non_functional:
  security:
    - 'The exemption admits one simple invocation only: benign cd/env prefixes, no command separators or substitution metacharacters in args, and nothing after the invocation (end-anchored).'
  reliability:
    - 'The hardened pattern fails closed: any shape it cannot prove simple is not exempted, so the worst case is a blocked human-visible reset (recoverable from the human shell), never an exempted compound remainder.'
contracts:
  - name: 'trap-exit-admission'
    type: 'contract-test'
    path: 'packages/caws-cli/tests/hooks/bats/block-dangerous.bats'
    description: 'While trapped: the simple, cd-prefixed, and env-prefixed reset invocations are admitted; compound remainders and metacharacter-bearing arguments are blocked with a strike.'
evidence:
  - criterion_id: A1
    status: pass
    evidence_ref: bats block-dangerous.bats anchor suite (32/32 green incl. ANCHOR A2-A4)
    recorded_at: "2026-09-12T19:51:49.269Z"
  - criterion_id: A2
    status: pass
    evidence_ref: bats block-dangerous.bats anchor suite (32/32 green incl. ANCHOR A2-A4)
    recorded_at: "2026-09-12T19:51:49.784Z"
  - criterion_id: A3
    status: pass
    evidence_ref: bats block-dangerous.bats anchor suite (32/32 green incl. ANCHOR A2-A4)
    recorded_at: "2026-09-12T19:51:50.354Z"
  - criterion_id: A4
    status: pass
    evidence_ref: bats block-dangerous.bats anchor suite (32/32 green incl. ANCHOR A2-A4)
    recorded_at: "2026-09-12T19:51:50.935Z"